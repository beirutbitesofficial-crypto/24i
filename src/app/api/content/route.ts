import{NextResponse}from"next/server";import{z}from"zod";import{authorize}from"@/lib/auth";import{db}from"@/lib/db";
const schema=z.object({clientId:z.string(),projectId:z.string().optional(),title:z.string().min(1).max(200),type:z.enum(["STATIC_POST","CAROUSEL","REEL","STORY","TIKTOK","YOUTUBE_SHORT","FACEBOOK_POST","LINKEDIN_POST","ADVERTISEMENT","OTHER"]),platform:z.array(z.string()).min(1),ownerId:z.string().optional()});export async function POST(req:Request){const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:p.error.flatten()},{status:400});const u=await authorize("content.write",p.data.clientId);const row=await db.$transaction(async tx=>{const c=await tx.contentItem.create({data:{...p.data,status:"IDEA"}});await tx.auditLog.create({data:{userId:u.id,action:"CONTENT_CREATED",entityType:"ContentItem",entityId:c.id,newValue:p.data}});return c});return NextResponse.json(row,{status:201})}
