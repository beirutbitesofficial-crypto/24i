import{NextResponse}from"next/server";import{z}from"zod";import{authorize}from"@/lib/auth";import{db}from"@/lib/db";import{money}from"@/lib/money";
const schema=z.object({profileId:z.string(),bonus:z.string().default("0"),deduction:z.string().default("0"),advance:z.string().default("0"),paymentDate:z.coerce.date(),notes:z.string().max(2000).optional()});export async function POST(req:Request){const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:p.error.flatten()},{status:400});const u=await authorize("finance.salaries.write"),profile=await db.salaryProfile.findUnique({where:{id:p.data.profileId}});if(!profile||!profile.active)return NextResponse.json({error:"Profile not found"},{status:404});const final=profile.baseSalary.plus(money(p.data.bonus)).minus(money(p.data.deduction)).minus(money(p.data.advance));if(final.isNegative())return NextResponse.json({error:"Final amount cannot be negative"},{status:400});const row=await db.$transaction(async tx=>{const s=await tx.salaryPayment.create({data:{profileId:profile.id,base:profile.baseSalary,bonus:money(p.data.bonus),deduction:money(p.data.deduction),advance:money(p.data.advance),finalAmount:final,paymentDate:p.data.paymentDate,notes:p.data.notes}});await tx.financialTransaction.create({data:{type:"SALARY",amount:final.negated(),salaryPaymentId:s.id,createdById:u.id}});await tx.auditLog.create({data:{userId:u.id,action:"SALARY_PAID",entityType:"SalaryPayment",entityId:s.id,newValue:{finalAmount:final.toString(),paymentDate:p.data.paymentDate}}});return s});return NextResponse.json(row,{status:201})}
