import{NextResponse}from"next/server";import{z}from"zod";import{authorize}from"@/lib/auth";import{db}from"@/lib/db";
const schema=z.object({clientId:z.string(),projectId:z.string().optional(),taskId:z.string().optional(),contentId:z.string().optional(),key:z.string().startsWith("clients/"),originalName:z.string().min(1),mimeType:z.string(),size:z.string().regex(/^\d+$/),checksum:z.string().optional()});export async function POST(req:Request){const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:p.error.flatten()},{status:400});const u=await authorize("files.write",p.data.clientId);if(!p.data.key.startsWith(`clients/${p.data.clientId}/`))return NextResponse.json({error:"Invalid storage scope"},{status:403});const file=await db.fileObject.create({data:{...p.data,size:BigInt(p.data.size),uploadedById:u.id}});await db.auditLog.create({data:{userId:u.id,action:"FILE_UPLOADED",entityType:"FileObject",entityId:file.id,newValue:{key:file.key,mimeType:file.mimeType,size:file.size.toString()}}});return NextResponse.json({...file,size:file.size.toString()},{status:201})}
